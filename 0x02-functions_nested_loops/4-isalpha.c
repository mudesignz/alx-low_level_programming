#include "main.h"
/**
 * _islower - check the code.
 * @c: is a single letter
 * Return: 1 if int c is lowercase, else 0
 */
int _isalpha(int c)
{
        if (c >= 'A' && c <= 'z')
        {
                return (1);
        }
        else
                return (0);
}
