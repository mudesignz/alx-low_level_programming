#include <stdio.h>
#include "main.h"
/**
 * print_most_numbers - check the code
 *
 * Return: Always 0.
 */
void print_most_numbers(void)
{
	int i, x;

	for (x = 0; x < 10; x++)
	{
		if (i = 0 && i < 14)
		{
			_putchar((i) + '0');
		}
	}
	_putchar('\n');
}
