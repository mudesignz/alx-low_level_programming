#include "main.h"

/**
 * mul - check the code.
 * @a: character input
 * @b: character input
 * Return: Always muliples . otherwise 0..
*/
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
