#include "main.h"
/**
 * _puts - entry point
 * @a: char data input by user
 *
 * Description: 'returns int to main program'
 * Return: Always 0 (Success)
 */
void _puts(char *a)
{
	char str[];
	str[] = *a;

	puts(a[]);
}
