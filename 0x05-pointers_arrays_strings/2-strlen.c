#include "main.h"
/**
 * strlen - entry point
 * @s: data input by user
 *
 * Description: 'returns int to main program'
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
	int c, n, count = 0;
	*s[n];
	for (c = 0; c <= n; c++)
	{
		count++;
	}
}
